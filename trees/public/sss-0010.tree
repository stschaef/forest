\title{FollowLast Set}
\taxon{Definition}
\date{2025-01-24}

\import{grammar-macros}

\p{
  The followlast set (#{\flast{\mtarg}}) of a grammar #{A} are all the characters that may follow a word in the language of #{A} in a string that is in the language of #{A}.

  ##{ \flast{A} = \mkset{c}{\exists w , w_A . w_A \in L(A) \land w_Acw \in L(A)}}
}
